// @shiage/runtime — the browser-side runtime, built as a single IIFE.
// The Shadow DOM overlay, element picker, style watcher, diff panel, and WS client
// are implemented in Phase 4. This placeholder keeps the package buildable.
export const RUNTIME_VERSION = '0.0.0'
